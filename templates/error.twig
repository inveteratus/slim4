{% extends "layouts/base.twig" %}

{% block body %}
    <main class="flex flex-grow items-center justify-center space-x-5 text-7xl font-thin">
        <span class="text-slate-700">{{ code }}</span>
        <span class="text-slate-500">{{ message }}</span>
    </main>
{% endblock %}
